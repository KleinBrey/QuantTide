import MarketStockBrowser from '@/features/market-stocks/components/MarketStockBrowser.jsx';

export default function MarketStocks({ marketId }) {
  return <MarketStockBrowser marketId={marketId} />;
}
